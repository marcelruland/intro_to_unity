/*
 * Apply to objects that may be hoarded.
 * Subset of throwables.
 * I.e.: Any item that can be hoarded can also bee thrown, but not vice versa!
 */

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Hord : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
